version: '3'

services:
  endpoint:
    restart: always
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - '3000:3000'
    networks:
      - app
    volumes:
      - './out:/app/out'

  ui:
    build:
      context: ui/
      dockerfile: Dockerfile
    ports:
      - '8000:80'
    networks:
      - app
    depends_on:
      - endpoint

  # extension:
  #   build:
  #     context: extension/
  #     dockerfile: Dockerfile
  #   # expose:
  #   #   - '9000'
  #   ports:
  #     - '9000:9000'
  #   networks:
  #     - app
  #   depends_on:
  #     - endpoint

networks:
  app:
