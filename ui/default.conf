upstream endpoint {
    server endpoint:3000;
}

server {
    listen 80;

    location / {
        root /assets;
        try_files $uri $uri/ /index.html;
    }

    location /api {
        rewrite /api/(.*) /$1 break;
        proxy_pass  http://endpoint;
    }
}